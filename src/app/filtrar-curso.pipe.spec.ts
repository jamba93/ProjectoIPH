import { FiltrarCursoPipe } from './filtrar-curso.pipe';

describe('FiltrarCursoPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltrarCursoPipe();
    expect(pipe).toBeTruthy();
  });
});
